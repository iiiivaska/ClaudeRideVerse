<!--
  This root-level file was an empty orphan. The canonical spec lives at:
  Architecture/PersistenceCore.md

  Keeping a stub here with a redirect so that any existing references
  (CLAUDE.md, external links, bookmarks) don't 404. Safe to delete once
  all references are updated.
-->

# PersistenceCore

See [[Architecture/PersistenceCore]] for the package spec.

Implementation: `Packages/PersistenceCore/` (GRDB 7.10, WAL, migrations).
